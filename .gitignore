target/
config.toml
# Not the app's config.toml above (a stray local copy of that is what this
# repo wants to keep out) -- cargo's own build config, committed for the
# /Brepro reproducible-build flag (issue #137, docs/reproducible-builds.md).
!.cargo/config.toml
*.log
.serena/
.claude/worktrees/
__pycache__/
# #203: tests/compile_fail.rs regenerates this from src/ui/confirm.rs on
# every run (see that file's doc comment for why); never hand-edited or
# committed.
tests/compile_fail/generated/
